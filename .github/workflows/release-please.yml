name: release-please

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: googleapis/release-please-action@v5
        with:
          token: ${{ secrets.PAT }}
          release-type: python
          target-branch: main
          # Labeling needs Issues write, which the fine-grained PAT does not have
          skip-labeling: true
